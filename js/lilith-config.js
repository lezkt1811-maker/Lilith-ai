/**
 * lilith-config.js
 * ------------------------------------------------------------------
 * The ONE line you need to edit after deploying your Cloudflare Worker.
 * This file is safe to commit to GitHub — it contains no secret, only
 * the public URL of your Worker (which itself refuses requests from
 * any origin except your GitHub Pages site, once ALLOWED_ORIGIN is set
 * in Cloudflare — see docs/SETUP-ANDROID.md).
 * ------------------------------------------------------------------
 */

window.LILITH_CONFIG = {
  // Paste your deployed Worker URL here, e.g.:
  // 'https://lilith-worker.YOUR-SUBDOMAIN.workers.dev'
  WORKER_URL: 'https://lilith-ai.lezkt1811.workers.dev',
};
